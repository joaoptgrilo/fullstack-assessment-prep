{
  "name": "fullstack-assessment-prep-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces"
  }
}
